--- 

interface Props {
  class?: string;
  size?: "default" | "narrow";
}

const { class: className = "", size = "default" }: Props = Astro.props;
const maxWidth = size === "narrow" ? "max-w-4xl" : "max-w-6xl";

---

<div class:list={["mx-auto w-full px-6", maxWidth, className]}>
  <slot />
</div>